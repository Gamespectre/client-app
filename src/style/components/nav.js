import colors from '../colors'

export const nav = {
    wrapper: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    },
    listItem: {
        padding: '0 1em'
    }
}
