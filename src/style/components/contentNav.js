import colors from '../colors'

export const contentNav = {
    bar: {
        background: colors.whitegrey,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    }
}